@import "../../app/vars.pcss" screen;

.Logo {
    flex: 1;
    padding-left: 40px;
    font: var(--LogoFont);

    &-First {
        display: inline-block;
        font-weight: bold;
        margin-right: 5px;
        background: var(--color);

        &::first-letter {
            text-transform: uppercase;
            color: red;
        }
    }

    &-Second {
        display: inline-block;

        &::first-letter {
            text-transform: uppercase;
        }

    }
}

.Navigation {

    &-item {
        width: 119px;
        text-align: center;
        opacity: 0.6;
        font: var(--ItemFont);
        color: var(--BlackColor);
        line-height: var(--NavItemLineHeight);

        &_active {
            opacity: 1;
        }
    }
}

.NavigationItemWrap {

    &-link {
        color: inherit;
        text-decoration: none;
    }
}

.Header {
    width: 1366px;
    display: flex;
    height: 92px;

    &-Logo {
        display: flex;
        align-items: center;
    }

    &-Navigation {
        flex: 6;
        display: flex;
        align-items: center;
    }
}

.HorizontalLine {
    background: #f0f0f0;
    border: none;
    height: 1px;
}
